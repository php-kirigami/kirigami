/node_modules/
*.tgz
/packs/
/.kirigami/

# esbuild output for packages/vscode's extension host bundle
packages/vscode/dist/

# plugin-embed regenerates this at every build (register() bridging a JS
# option into a static SCSS file) — see packages/plugin-embed/index.js
packages/plugin-embed/.generated-vars.scss

# Local backups (e.g. packages/php-wasm/jspi/php.js.bak) must never be committed or published
*.bak
